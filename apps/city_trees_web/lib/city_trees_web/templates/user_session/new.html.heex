<AuthLayout.layout title="Log in">
  <.form let={f} for={@conn} class="space-y-4" action={Routes.user_session_path(@conn, :create)} as={:user}>
    <%= if @error_message do %>
      <TailwindAlert.error message={@error_message} />
    <% end %>

    <TailwindForm.email_input form={f} />
    <TailwindForm.password_input form={f} />

    <TailwindForm.checkbox 
      form={f} 
      field={:remember_me} 
      label="Keep me logged in for 60 days"
    />
    
    <TailwindButton.primary type="submit" size={:medium}>Log in</TailwindButton.primary>
  </.form>

  <p class="pt-2"> 
    <%= link "Register", to: Routes.user_registration_path(@conn, :new) %> |
    <%= link "Forgot your password?", to: Routes.user_reset_password_path(@conn, :new) %>
  </p>
</AuthLayout.layout>